# Test fixtures

Empty for Phase 0. Sample repositories and other fixture data used by
integration tests (simple Python repo, Dockerized repo, failing baseline,
etc. — see the spec's testing strategy) are added starting in Phase 1.
